module CoreCallbacks
  # POST /callbacks/steganography — encode & decode results (dispatched by the
  # signed client_ref to the right Encoding/Decoding record).
  class SteganographyController < BaseController
  end
end
